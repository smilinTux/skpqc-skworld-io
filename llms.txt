# sk_pqc

> Hybrid post-quantum key encapsulation for Dart & Flutter. X25519 + ML-KEM-768
> (FIPS 203), one Dart API across web and native backends. Suite id:
> x25519-mlkem768 — the same construction as TLS X25519MLKEM768 and Signal PQXDH.

sk_pqc is a hybrid Key Encapsulation Mechanism (KEM). It establishes a 32-byte
shared secret that is secure if EITHER the classical leg (X25519) OR the
post-quantum leg (ML-KEM-768) holds. It does not replace classical crypto; it
combines the two.

## Honest scope (do not overclaim)

- It is a hybrid KEM, secure if either leg holds — not "quantum-proof" or
  "unbreakable." Defensible words: "post-quantum" / "quantum-resistant."
- It is KEM-only. Signatures (ML-DSA / SLH-DSA) are future work. It does not
  authenticate anything by itself.
- It targets the FIPS 203 ML-KEM-768 tier (the internet default), NOT the
  CNSA-2.0 ceiling (ML-KEM-1024).
- The ONLY original cryptographic code is the HKDF-SHA256 combiner. Every
  primitive binds an audited library: noble-post-quantum (web), liboqs (native),
  package:cryptography (X25519), RFC 5869 HKDF.
- Web-leg assurance depends on @noble/post-quantum; native-leg on liboqs.

## The combiner (the only crypto we wrote)

shared_secret = HKDF-SHA256( IKM = X25519_ss || MLKEM768_ss, salt, info, L=32 )
X25519 part first. salt defaults empty. info defaults sk_pqc/x25519-mlkem768/v1.
Concatenate-then-KDF. Never XOR. Never pure-PQ.

## Wire format (fixed lengths)

- public key:    X25519_pub(32) || MLKEM768_pub(1184)        = 1216 bytes
- private key:   X25519_priv_seed(32) || MLKEM768_secret(2400)= 2432 bytes
- ciphertext:    X25519_ephemeral_pub(32) || MLKEM768_ct(1088)= 1120 bytes
- shared secret: HKDF-SHA256(...)                             = 32 bytes

## API

import 'package:sk_pqc/sk_pqc.dart';
final kem  = HybridKemImpl();                  // backend auto-selected
final keys = await kem.generateKeyPair();
final enc  = await kem.encapsulate(keys.publicKey);
final ss   = await kem.decapsulate(enc.ciphertext, keys.privateKey);

## Verified

- FIPS 203 KAT (ML-KEM-768, NIST ACVP keyGen seed d||z, tcId 26)
- RFC 5869 §A.1 KAT (HKDF combiner)
- cross-backend: web (noble) <-> native (liboqs) agree
- Dart <-> Python interop vector (pyca + liboqs-python + HKDF-SHA256)

## Interop matrix (Python / Dart / Rust)

KEM and DM epoch ratchet are wire-verified in ALL THREE languages against one
shared cross-implementation KAT. Higher-layer primitives (group ratchet, PQ
message seal/pqdm, pqroute routing envelope, anon_queue addressing, suite
registry + honest self-report) are implemented and Python<->Rust parity-tested
today, and reach Dart via the single Rust core over FFI (in progress). A check
means a passing parity test, not a plan. Cross-node proven: web(noble) <->
native(liboqs) agree; the Rust PyO3 wheel <-> pure-Python sk_pqc decapsulate
each other both directions; skchat/skcomms negotiate hybrid PQ DMs in-browser
(verified end-to-end over CDP).

## Benchmarks (reproduce on your own hardware)

Whole hybrid-KEM operation (X25519 leg + ML-KEM-768 leg + HKDF combiner), via
examples/bench.py (timeit, 500 iters x 5, median), Python 3.12 / Linux x86-64,
no GPU:
- keygen ~226 us (mean 279) ~4,400 ops/sec
- encap  ~360 us (mean 512) ~2,800 ops/sec
- decap  ~341 us (mean 416) ~2,900 ops/sec
Sub-millisecond; ML-KEM-768 leg dominates. Paid once per ratchet epoch, not per
message. Run: python examples/bench.py [iters].

## Single core, many bindings (write the crypto once)

Direction is ONE audited pure-Rust core (sk-pqc-rs: ml-kem + x25519-dalek, no
OpenSSL, no liboqs; 9 modules, 99 in-tree tests) surfaced over FFI:
- Python via PyO3 (abi3 wheel): PROVEN — cross-parity-tested vs pure-Python
  sk_pqc, both directions.
- Dart via flutter_rust_bridge: IN PROGRESS — today's Dart ratchet is a thin,
  clearly-marked bridge pinned to the same KAT, slated to become a shim over the
  core. PyO3 feature is off by default so cargo test stays pure-Rust and
  crates.io stays dependency-clean.

## Threat model / HNDL

Built against harvest-now-decrypt-later (HNDL). The -768 hybrid PROTECTS:
recorded-traffic confidentiality (breaking X25519 later still leaves ML-KEM-768),
classical-break-today (lattice surprise still leaves X25519), forward secrecy
across ratchet epochs; AES-256-GCM is Grover-only and already acceptable. It
does NOT: authenticate (KEM only; FIPS 204 ML-DSA signatures gated as future
work; active MITM out of scope for the KEM alone), reach CNSA-2.0 (deliberately
-768, not -1024), fix HNDL for a classical-only ratchet, or substitute for a
third-party audit (none yet) or endpoint security. Hybrid = confidential as long
as EITHER leg holds. Never "quantum-proof"/"quantum-safe"/"unbreakable" — the
report module mechanically rejects those words.

## Links

- Site: https://skpqc.skworld.io
- Source: https://github.com/smilinTux/sk_pqc
- License: Apache-2.0
- Ecosystem: https://skworld.io (SKWorld quantum-resistance initiative)

## Status

- Hybrid combiner (HKDF-SHA256): tested vs RFC 5869 + vectors
- Native FFI (liboqs) ML-KEM-768: runs on Linux desktop
- Web (noble) ML-KEM-768: compiles under dart2js, cross-checked vs liboqs
- Per-arch native binaries (Android/iOS/mac/Win): CI follow-up
- Signatures (ML-DSA / SLH-DSA): out of scope (future)
- pub.dev publish: pending (depend from GitHub for now)
